# ===============================
#  .env.example
#  プロジェクトに必要な環境変数の見本
#  ※実際の値は.envに記入するか、デプロイ先の環境変数に設定してください
# ===============================

# --- Discord Bot 関連 ---
TOKEN=<Your Discord Bot Token>               # Discord Botのトークン
APPLICATION_ID=<Your Application ID>         # DiscordアプリケーションID
OWNER_ID=<Your Discord Account ID>           # Bot管理者のDiscordアカウントID
GUILD_IDS=<Guild ID 1>,<Guild ID 2>          # ギルドコマンドを登録するサーバーID（カンマ区切り）

# --- データベース接続 (Sequelize/pg用) ---
# DBにスーパーユーザーとして直接接続するための文字列
# パスワード部分にはSupabaseダッシュボードで設定した「データベースパスワード」が入る
DATABASE_URL=postgresql://postgres:<Your DB Password>@<Your Supabase Host>/postgres

# --- Supabase API接続 (@supabase/supabase-js用) ---
# StorageなどDB以外の機能に管理者としてアクセスするために使用
SUPABASE_SERVICE_KEY=<Your Supabase Service Role Key (the long one)>
SUPABASE_API_URL=<Your Supabase Project URL>

# --- stikersで上げたスタンプの一覧HTMLを上げておくためのgithubレポジトリ
# このリポジトリとは別のMariachang-pagesとかのを作ってそれだけ許可するトークン発行してもらうといいかも後は調べて
GH_PAGES_TOKEN=<Your github token>

# --- Mee6 API接続 ---
MEE6_GUILD_ID=<Your Guild ID>               # Mee6の情報を取得したいサーバーID

# --- その他 ---
SERVICE_NAME=<Example: koyeb>                # Ready時に稼働先サーバ名を表示するための値
TZ=Asia/Tokyo                                # タイムゾーン設定
backup_command=<Backup code>                 # sqlite時代の名残（未使用）
kampa=<Your Fund URL>                        # /kampa で表示する支援先URL
NODE_ENV=production     #本番環境ならこう、別に無くても動くが。